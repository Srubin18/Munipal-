import { AiployeePM } from "@/components/property-management";

export const metadata = {
  title: "AIPLOYEE Property Manager",
  description: "AI-powered property management — tenants, rent collection, vacancy & leads",
};

export default function PropertyManagementPage() {
  return <AiployeePM />;
}
